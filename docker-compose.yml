services:
  notification:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: itmo-notification:baseline
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/health"]
      interval: 5s
      timeout: 2s
      retries: 5
